import { useState } from "react";

const Home = () => {
  return (
    <div className="home">
        <h4 className="homepageText">
            bus control tool - click links in navbar to see a dashboard of bus trip or to login to record a cancellation
        </h4>
    </div>
  );
};

export default Home;